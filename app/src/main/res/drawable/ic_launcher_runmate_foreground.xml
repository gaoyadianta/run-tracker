<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The raster itself contains padding; this inset keeps the runner clear
         under common launcher masks without making it unreadably small. -->
    <item
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp">
        <bitmap
            android:antialias="true"
            android:dither="true"
            android:filter="true"
            android:gravity="fill"
            android:src="@drawable/ic_launcher_runmate_art" />
    </item>
</layer-list>
